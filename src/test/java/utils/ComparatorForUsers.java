package utils;

import models.User;

import java.util.Comparator;

public class ComparatorForUsers {
   // static class UserComparator implements Comparator<User> {

//        public boolean compare(User user, User expectedUser) {
//            if (user.getName().equals(expectedUser.getName()) && user.getUserName().equals(expectedUser.getUserName())
//                    && user.getEmail().equals(expectedUser.getEmail() && user.getEmail() ))
//
//
//            return a.getStartTime().compareTo(b.getStartTime());
//        }
//
//
////    }
}
